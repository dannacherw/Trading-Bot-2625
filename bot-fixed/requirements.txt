# Core async
aiohttp>=3.9.1
aiosqlite>=0.19.0
websockets>=12.0

# HTTP
httpx>=0.25.2

# Data / numerics
numpy>=1.26.2
pandas>=2.1.4
scipy>=1.11.4

# Modeling / validation
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Config
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Logging
loguru>=0.7.2

# CLI
click>=8.1.7
rich>=13.7.0
tabulate>=0.9.0

# Testing
pytest>=7.4.3
pytest-asyncio>=0.23.2
pytest-mock>=3.12.0
pytest-cov>=4.1.0
freezegun>=1.4.0
aioresponses>=0.7.6
factory-boy>=3.3.0

# Dev tooling
black>=23.12.0
ruff>=0.1.8
mypy>=1.7.1

# v4 additions
yfinance>=0.2.36          # Yahoo Finance fallback for float data
aiohttp>=3.9.1            # already listed — kept for float_provider async sessions

# API backend
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
aiosqlite>=0.20.0

# Google Sheets integration
# google-auth is optional — falls back to openssl subprocess if absent
# cryptography>=42.0.0   # recommended for faster JWT signing

# Optional but recommended
httpx>=0.27.0           # for FastAPI TestClient in tests
